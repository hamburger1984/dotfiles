
# -- get source

# wget latest python*.tar.xz
# tar xvf *.tar.xz


# -- configure, make

# cd Python-...
# ./configure --prefix=$HOME/opt --exec_prefix=$HOME/opt
# make -j -l 3 (cores|threads+1)

# -- install

# make install
#
# .. no ~/bin/ yet? do this:
# ln -s ~/opt/bin/ ~/bin
# .. else
# ln -s ~/opt/bin/python ~/bin/python (for all needed files in ~/opt/bin/)

# -- verify

# python3 --version

