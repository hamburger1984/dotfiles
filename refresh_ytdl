#!/bin/sh

find . -type f -name .ytdlsource | while read SOURCE
do
    echo $SOURCE
    pushd $(dirname $SOURCE) > /dev/null
    pwd
    youtube-dl -t -i -c -w -x -a $(basename $SOURCE)
    popd > /dev/null
done

